<!DOCTYPE html>
<html lang="pt-BR">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Barlow+Condensed:wght@400;700&display=swap" rel="stylesheet">
  <title>Relatorio</title>
  <style>
    * {
      margin: 0;
      border: 0;
      box-sizing: border-box;
      font-family: 'Barlow Condensed', sans-serif;
    }

    body {
      background: #1e1e1e;
    }

    .main {
      width: 100vw;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 3rem;
      gap: 3rem;
    }

    .main-text {
      text-align: left;
      width: 50%;
      color: #f2f2f2;
      font-size: 24px;
      line-height: 48px;
    }

    .main-text h1 {
      font-size: 45px;
    }

    .login-div-wrapper {
      background: linear-gradient(245deg, #00e7ff 40%, #f7855e 71%, #fe2e36 91%);
      width: 400px;
      max-width: 100%;
      align-self: stretch;
      padding: 2px;
      border-radius: 8px;
    }

    .login-div {
      width: 100%;
      height: 100%;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 2rem;
      border-radius: 8px;
      box-shadow: rgba(50, 50, 93, 0.25) 0px 6px 12px -2px, rgba(0, 0, 0, 0.3) 0px 3px 7px -3px;
      padding: 3rem;
      background: #1e1e1e;
    }


    .btn-wrapper {
      margin-top: 5vh;
      border-radius: 100px;
      padding: 2px;
      background: linear-gradient(245deg, #00e7ff 40%, #f7855e 71%, #fe2e36 91%);
    }

    .btn {
      border-radius: 100px;
      padding: 12px 30px;
      width: 100%;
      text-align: center;
      background: #1e1e1e;
      cursor: pointer;
      transition: all .2s ease-in-out;
      color: #fff;
      font-size: 1.2rem;
    }


    .btn:hover {
      background: none;
    }

    .input-container {
      width: 100%;
      display: flex;
      align-items: center;
      justify-content: center;
      background: #1e1e1e;
      border-bottom: 1px solid white;
      position: relative;
    }

    .input-container input {
      width: 100%;
      align-self: stretch;
      padding: .8rem .5rem .5rem;
      border: 0;
      background: #1e1e1e;
      color: white;
      font-size: 1.2rem;
      letter-spacing: 2px;
    }

    .input-container input:focus {
      outline: none;
    }

    .input-container input:focus~label {
      top: -20%;
      font-size: 14px;
    }

    .input-container label {
      position: absolute;
      left: 10px;
      top: 26%;
      transition: all .2s ease-in-out;
      color: #808080;
    }

    .input-container label.animate {
      top: -20%;
      font-size: 14px;
    }

    .logo {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
    }

    .logo .logo-text {
      font-size: 2rem;
      color: #fff;
    }

    .logo .logo-img-text {
      display: flex;
      align-items: center;
      justify-content: center;
      flex-direction: column;
      padding-top: 10px;
      color: #fff;
    }

    @media (max-width: 900px) {
      .main-text {
        display: none;
      }
    }

    .errors {
      color: red;
      font-size: 1.2rem;
      animation: error 1s ease forwards;
    }

    @keyframes error {
      25% {
        transform: translateX(-5px);
      }
      50% {
        transform: translateX(5px);
      }
      75% {
        transform: translateX(-5px);
      }
      100% {
        transform: translateX(0);
      }
    }
 </style>
</head>

<body>
  <section class="main">
    <div class="main-text">
      <h1>
        Relatório <br />de transfêrencia de dados.
      </h1>
      <p> Parque da Mônica. </p>
    </div>
    <div class="login-div-wrapper">
      <div class="login-div">

        <div class="logo">
          <div class="logo-text">CÍRCULO</div>
          <div class="logo-img-text">
            <img src="https://www.circulocrm.com.br/static/8dc455e1074fc39808bc5dbdc851cf40/ad39b/logo-min.png" alt="">
            <span>
              crm
            </span>
          </div>
        </div>

        <div class="errors">
          Credenciais inválidas
        </div>


        <div class="input-container">
          <input class="animate-input" type="email">
          <label for="">
            E-mail
          </label>
        </div>

        <div class="input-container">
          <input class="animate-input" type="password">
          <label for="">
            Senha
          </label>
        </div>

        <div class="btn-wrapper">
          <button class="btn" type="submit">Login</button>
        </div>
      </div>
    </div>
  </section>

  <script>
    const passwordInput = document.querySelectorAll('.animate-input');

    passwordInput.forEach(input => {
      input.onkeypress = () => {
        input.nextElementSibling.classList.add('animate');

        if (!input.value) {
          input.nextElementSibling.classList.remove('animate');
        }
      };
    })


  </script>
</body>

</html>